server:
  port: 8080

spring:
  h2:
    console:
      enabled: true
      path: /h2
  jpa:
    show-sql: true
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:


management:
  endpoints:
    web:
      exposure:
        include:
          - beans
          - health
          - metrics
          - env
